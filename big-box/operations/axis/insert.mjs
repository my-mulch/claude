[[1, 1],
[2, 2],
[3, 3]]

// shape [3,2] 
// insert axis=0
// rows (0,1)
// data [7,7]

for (let r = 0; r < a.shape[0]; r++) // this goes over entirety of new structure
    for (let c = 0; c < a.shape[1]; c++)

    // we sometimes assign from data
    // and sometimes from the original array
    // what are the cases?

    // well if we are in a row passed to us from `rows`
    // then we should assign from data
    { } { }
